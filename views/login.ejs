<!DOCTYPE html>
<html lang="en">
    <%-include ("./partials/head")%>
<body>
    <%-include ("./partials/header")%>

<main class="main-login-registrarse">
    <div class="div-formularios-monitor">    
        <form action="/users/login" method="POST" class="formulario-login">
            
            <h2>Ingresa a tu cuenta.</h2>
            <p>Bienvenido. Ingresa tus datos</p>
            <form action="/users/perfil" method="POST">
                <input class="boton-login" type="email" placeholder="E-mail" name="email">
                <% if (locals.errors && errors.email) { %>
                    <div class="text-validation">
                        <%= errors.email.msg %>
                    </div>
                    <% } %>
                <input class="boton-login" type="password" placeholder="Contraseña" name="clave">
                <% if (locals.errors && errors.clave) { %>
                    <div class="text-validation">
                        <%= errors.clave.msg %>
                    </div>
                    <% } %>
                <input class="boton-login-registrar" type="submit" value="INGRESAR">
            <form>
            <a href="#">¿Olvidaste tu contraseña?</a>
            <label>
                <input type="checkbox" name="remember_user"> Recordar mi usuario
            </label>
            <a href="/users/login/register" id="buttons-cuenta">Crea tu cuenta</a>
        </form>
    </div>
        <%-include ('./partials/cartas-beneficios') %>
</main>
        <%-include ("./partials/footer")%>
</body>
</html>